function divider(input)
{
    const spacer = `=`.repeat(20);
    console.log(`\n` + spacer, input, spacer, `\n`);
}

divider("IF Vergleich")

const ergebnis = 5;
if(ergebnis <= 5) {
console.log(`ich werde nur angezeigt, wenn das if statement zutrifft!`);
} 

